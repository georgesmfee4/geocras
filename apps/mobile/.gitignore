# Projets natifs générés par `expo prebuild` / `expo run:*`.
#
# On reste en CNG : `app.json` est la source de vérité, EAS régénère `android/`
# et `ios/` à chaque build. Les commiter ferait passer EAS en bare workflow — il
# ignorerait alors les config plugins (splash, permissions, MapLibre) et les
# builds divergeraient de la configuration.
android/
ios/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli